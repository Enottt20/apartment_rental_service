version: '1.0'
services:
  postgresql:
    image: postgis/postgis:latest
    container_name: postgis
    environment:
      POSTGRES_DB: postgres  # Имя вашей базы данных
      POSTGRES_USER: postgres    # Имя пользователя базы данных
      POSTGRES_PASSWORD: 1023 # Пароль пользователя базы данных
    ports:
      - 5432:5432  # Маппинг портов (локальный порт: порт в контейнере)
    volumes:
      - /path/to/local/data:/var/lib/postgresql/data  # Монтирование тома для сохранения данных

  favorite-service:
    image: favorite-service:1.0
    ports:
      - "5001:5001"
    environment:
      PG_DSN: ${POSTGRES_DSN}

  apartment-service:
    image: apartment-service:1.0
    ports:
      - "5002:5002"
    environment:
      PG_DSN: ${POSTGRES_DSN}


volumes:
  postgres_data: